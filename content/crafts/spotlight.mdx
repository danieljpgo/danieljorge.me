---
title: Spotlight Effect
description: lorem
createdAt: "2023-05-12"
status: "published"
topics: ["ui", "web", "css"]
---

<Playground prefer="pointer">
  <Spotlight />
</Playground>

---

Second

Primeiro devemos desenvolver o a superfice onde o Spotlight Effect irá ocorer:

<Playground>
  <SpotlightFirst />
</Playground>

```tsx showLineNumbers title="Spotlight.tsx"
export function Spotlight() {
  return <div className="spotlight" />;
}
```

```css showLineNumbers title="styles.css"
.spotlight {
  background: #181e33;
  border-radius: 0.75rem;
  border-width: 1px;
  border: solid 1px rgb(255 255 255 / 0.1);
  box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.25);
  max-width: 24rem;
  min-height: 15.5rem;
  padding: 2.5rem 2rem;
  width: 100%;
}
```

---

Second

Em seguida, devemos adicionar o elemento que será responsavel pela movimentação, no caso estamos utilizando o um [pseudo-element](https://developer.mozilla.org/en-US/docs/Web/CSS/Pseudo-elements) para isso, porém pode utilizar qualquer um que desejar:

<Playground>
  <SpotlightSecond />
</Playground>

```css showLineNumbers title="styles.css" {3,4}
.spotlight-second {
  /* ... */
+  position: relative;
+  z-index: 1;
}

.spotlight::before {
  background: radial-gradient(
    250px at center,
    rgba(130, 147, 248, 1),
    transparent 80%
  );
  border-radius: 0.75rem;
  content: "";
  inset: 0;
  position: absolute;
  transition-duration: 0.3s;
  transition-property: background-color, opacity;
  transition-timing-function: cubic-bezier(0.4, 0, 0.2, 1);
  z-index: -1;
}
```

---

Third

Agora, para realizar a movimentação, vamos utilizar [CSS variables](https://developer.mozilla.org/en-US/docs/Web/CSS/Using_CSS_custom_properties) para guardar a posição atual do mouse.
Essa abordagem permitirar é mais performatica pois não depende de nenhum estado do React ou qualquer framework que esteja utilizando, além de não precisar de nenhuma biblioteca de animação.

<Playground>
  <SpotlightThird />
</Playground>

Como estamos utilizando React, vamos utilizar o `useRef` para ter a referencia do elemento no DOM e podemos manipular.
No eventhandler do mousemove, vamos calcular a posição radial-gradient utilizando as coordenadas do mouse por meio dos `MouseEvent.clientX` e `MouseEvent.clientY`, em relação a [viewport](https://developer.mozilla.org/en-US/docs/Glossary/Viewport) por meio dos valores de `x` e `y` do `getBoundingClientRect()`.

Por fim, vamos armazenar os valores encontrados em CSS variables para informar ao CSS qual a posição que o radial-gradient deve tomar.

```tsx showLineNumbers title="Spotlight.tsx" 
export function Spotlight() {
  const ref = useRef<ElementRef<"div">>(null);

  function handleMouseMove(event: React.MouseEvent<ElementRef<"div">>) {
    if (!ref.current) return;
    const { x, y } = event.currentTarget.getBoundingClientRect();

    ref.current.style.setProperty("--x", String(event.clientX - x));
    ref.current.style.setProperty("--y", String(event.clientY - y));
  }

  return <div ref={ref} className="spotlight" onMouseMove={handleMouseMove} />;
}
```

Agora com os valores sendo da posição sendo adicionados, podemos acessa-los dentros do CSS e definindo a posição do radial-gradient em relação ao background do elemento.
Um truque para facilitar a conversão dos valores para pixels é utilizar a função calc().

{/* /var(--x, 0)/ /var(--y, 0)/ */}
```css showLineNumbers title="styles.css" {7,8}
.spotlight {
  /* ... */
}

.spotlight::before {
  background: radial-gradient(
-   250px at center,
+   250px at calc(var(--x, 0) * 1px) calc(var(--y, 0) * 1px),
    rgba(130, 147, 248, 1),
    transparent 80%
  );
  /* ... */
}
```


---

Quarto

Para finalizar, devemos esconder o radial-gradient até o mouse está em cima do nosso card, para isso podemos utilizar a opacidade.

<Playground>
  <SpotlightFourth />
</Playground>

```css showLineNumbers title="styles.css" {8-11}
.spotlight {
  /* ... */
}

.spotlight::before {
  /* ... */
  background: radial-gradient(
-    250px at calc(var(--x, 0) * 1px) calc(var(--y, 0) * 1px),
+    550px at calc(var(--x, 0) * 1px) calc(var(--y, 0) * 1px),
-    rgba(130, 147, 248, 1),
+    rgba(130, 147, 248, 0.25),
    transparent 80%
  );
  opacity: 0;
}

.spotlight:hover::before {
  opacity: 1;
}
```

---

Final

<Playground prefer="pointer">
  <Spotlight />
</Playground>

```tsx showLineNumbers title="Spotlight.tsx"
export function Spotlight() {
  const ref = useRef<ElementRef<"div">>(null);

  function handleMouseMove(event: MouseEvent<ElementRef<"div">>) {
    if (!ref.current) return;
    const { x, y } = event.currentTarget.getBoundingClientRect();

    ref.current.style.setProperty("--x", String(event.clientX - x));
    ref.current.style.setProperty("--y", String(event.clientY - y));
  }

  return (
    <div ref={ref} className="craft-spotlight" onMouseMove={handleMouseMove}>
      {...}
    </div>
  );
}
```

```css showLineNumbers title="styles.css"
.spotlight {
  background: #181e33;
  border-radius: 0.75rem;
  border-width: 1px;
  border: solid 1px rgb(255 255 255 / 0.1);
  box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.25);
  max-width: 24rem;
  min-height: 15.5rem;
  padding: 2.5rem 2rem;
  position: relative;
  width: 100%;
  z-index: 1;
}

.spotlight::before {
  background: radial-gradient(
    550px at calc(var(--x, 0) * 1px) calc(var(--y, 0) * 1px),
    rgba(130, 147, 248, 0.25),
    transparent 80%
  );
  border-radius: 0.75rem;
  content: "";
  inset: 0;
  opacity: 0;
  position: absolute;
  transition-duration: 0.3s;
  transition-property: background-color, opacity;
  transition-timing-function: cubic-bezier(0.4, 0, 0.2, 1);
  z-index: -1;
}

.spotlight:hover::before {
  opacity: 1;
}
```
